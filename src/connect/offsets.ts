/** Sensor distance behind the bit in the replay feed (m); its time-to-depth step uses the same offsets. */
export const REPLAY_OFFSETS: Record<string, number> = { GR: 11.8, RDEP: 14.6, RHOB: 21.9, NPHI: 23.4 };
